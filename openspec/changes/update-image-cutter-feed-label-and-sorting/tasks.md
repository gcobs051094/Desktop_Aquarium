## 1. 更新分類選單標籤
- [x] 1.1 將分類依據下拉選單中的「飼料」改為「飼料 & 錢」
- [x] 1.2 確認內部對應的 `classify_basis` 值仍為 `"feed"`（索引 2）

## 2. 簡化 DFS 檢測階段的排序邏輯
- [x] 2.1 移除 `detect_dfs_regions` 中複雜的行分組邏輯
- [x] 2.2 改為直接依左邊界 x 座標排序：`sorted(regions, key=lambda r: r[0])`
- [x] 2.3 移除中心點計算、行容差計算、行分組等邏輯

## 3. 更新裁切階段的排序邏輯
- [x] 3.1 在 `start_dfs_cropping` 中，將區域排序改為依左邊界 x 座標
- [x] 3.2 自訂類別模式：移除對 `rows` 的依賴，直接使用已排序的 `regions_sorted`
- [x] 3.3 自訂類別模式：將類別內排序改為由左往右（移除先按 top 再按 left 的邏輯）
- [x] 3.4 一般輸出模式：移除對 `rows` 的依賴，直接使用已排序的 `regions_sorted`
- [x] 3.5 魚分類模式：保留行分組邏輯（因為需要對應行為資料夾），但從 `regions_sorted` 重新分組

## 4. 測試和驗證
- [ ] 4.1 測試分類選單顯示「飼料 & 錢」
- [ ] 4.2 測試 DFS 檢測後的區域編號順序為由左往右
- [ ] 4.3 測試自訂類別模式下儲存順序為由左往右
- [ ] 4.4 測試一般輸出模式下儲存順序為由左往右
- [ ] 4.5 測試魚分類模式仍能正確對應行為資料夾
