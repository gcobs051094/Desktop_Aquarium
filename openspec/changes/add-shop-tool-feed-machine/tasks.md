## 1. 商店新增「工具」tab
- [ ] 1.1 在 `ShopOverlay` 新增「工具」tab（QScrollArea + 垂直卡片列表 layout）
- [ ] 1.2 在 `ShopOverlay.update_items()` 增加工具 tab 的更新流程（傳入工具解鎖/顏色狀態或由主視窗提供 getter）

## 2. 工具：飼料投食機商品卡片與顏色下拉
- [ ] 2.1 建立投食機顏色清單與對應素材檔名（預設 `灰黑色`）
- [ ] 2.2 未解鎖狀態：顯示解鎖條件「曾達到 10 隻成年孔雀魚」（對應孔雀魚數量里程碑），並禁用顏色操作
- [ ] 2.3 已解鎖狀態：顯示顏色下拉式選單（ComboBox），切換時即時更新水族箱投食機顏色

## 3. 解鎖條件與狀態管理
- [ ] 3.1 以 `unlocked_species` 的里程碑（`max_count_reached`）判定投食機解鎖
- [ ] 3.2 決定並實作「成年孔雀魚」對應鍵（預設以 `孔雀魚` 里程碑為準），並在 UI 文案註明為里程碑判定

## 4. 水族箱顯示投食機素材
- [ ] 4.1 在水族箱加入投食機顯示狀態（是否顯示 + 當前顏色 pixmap）
- [ ] 4.2 在 `AquariumWidget.paintEvent()` 於座標 (25, 240) 繪製投食機圖片（解鎖後顯示）
- [ ] 4.3 驗證投食機繪製層級（背景之上、魚/飼料/金錢/寵物之下或之上）符合預期並固定一致

## 5. 存檔/載入（向後相容）
- [ ] 5.1 在存檔 state 新增工具相關欄位（如 `tools`/`feed_machine_color`）
- [ ] 5.2 載入舊存檔缺欄位時使用預設值（灰黑色、未顯示/未解鎖）
- [ ] 5.3 觸發顏色變更後自動儲存（沿用現有 `_auto_save()`）

